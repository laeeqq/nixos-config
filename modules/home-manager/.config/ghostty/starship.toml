"$schema" = 'https://starship.rs/config-schema.json'

# ─ Top table above prompt
[custom.top_table]
command = """
printf "┌─────────────┬───────────────┬───────────────┐\n"
printf "│ Dir         │ Git Branch    │ Term Info     │\n"
printf "├─────────────┼───────────────┼───────────────┤\n"
printf "│ %-11s │ %-13s │ %-13s │\n" "$(basename "$PWD")" "$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo 'N/A')" "Ghostty"
printf "└─────────────┴───────────────┴───────────────┘\n"
"""
style = "bold fg:#FF6F00 bg:#1C1C1C"

# ─ Prompt layout
format = """
$username\
[](fg:#1C1C1C bg:#2C2C2C)$directory\
[](fg:#2C2C2C bg:#FF6F00)$git_branch$git_status\
[](fg:#FF6F00 bg:#1C1C1C)$python$c$cpp$rust$golang$java$nodejs\
[](fg:#1C1C1C bg:#2C2C2C)$time\
$line_break$character
"""

# ─ Username
[username]
show_always = true
style_user = "bold fg:#FFA000 bg:#1C1C1C"
format = " [ $user ]($style) "

# ─ Directory
[directory]
style = "bold fg:#E0E0E0 bg:#2C2C2C"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"

# ─ Git branch
[git_branch]
symbol = ""
style = "bold fg:#E0E0E0 bg:#FF6F00"
format = "[$symbol $branch]($style)"

[git_status]
style = "bold fg:#E0E0E0 bg:#FF6F00"
format = "[$all_status$ahead_behind]($style)"

# ─ Right-hand panel: first 5 files
[custom.ghostty_info]
command = """
echo -n "Files: "
ls -1A | head -n 5 | tr '\n' ' '
"""
style = "bold fg:#FFA000 bg:#1C1C1C"

# ─ Time
[time]
disabled = false
time_format = "%R"
style = "fg:#E0E0E0 bg:#2C2C2C"
format = "🕒 $time "

# ─ Prompt character
[character]
success_symbol = "❯"
error_symbol = "✗"
